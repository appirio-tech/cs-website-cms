development:
  adapter: postgresql
  database: cs_website_cms_development
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: cs_website_cms_test
  host: localhost

# this is for testing and precompiling assets locally. Heroku overwrite this file during deploy
production:
  adapter: postgresql
  database: cs_website_cms_development
  host: localhost