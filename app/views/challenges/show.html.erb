<div class="content-wrapper">
  <div class="container">

    <div class="title"><h1>CHALLENGE DETAIL</h1></div>

    <div class="row-fluid">

      <div class="span8">
        <h1><%= @challenge.name %></h1>

        <h2>OVERVIEW</h2>
        <p> <%= @challenge.description.html_safe %> </p>

        <h2>REQUIREMENTS</h2>
        <p> <%= @challenge.requirements.html_safe %> </p>
        
        <% if @challenge.submission_details %>
          <h2>SUBMISSION DETAILS</h2>
          <p> <%= @challenge.submission_details.try(:html_safe) %> </p>
        <% end %>

        <% if @challenge.additional_info %>
          <h2>ADDITIONAL INFO</h2>
          <p> <%= @challenge.additional_info.try(:html_safe) %> </p>
        <% end %>

        <br/>

        <h2 class="discussion">DISCUSSION</h2>
        <%= render partial: "discussions", locals: {challenge: @challenge} %>
      </div>

      <div class="span4">

        <% unless @challenge.challenge_type.eql?("Info Only - No Registration") %>
        <h2>GET STARTED</h2>

          <p>
          <% if @current_member_participant.submitted? %>
              <% if user_signed_in? %>
                <%= link_to content_tag(:span, "VIEW YOUR SUBMISSION"), register_challenge_path(@challenge), class: "btn" %>
              <% end %>
          <% elsif @challenge.open? %>   
            <div>
            <% if user_signed_in? %>
              <% if !@current_member_participant.registered? && !@challenge.closed_for_registration? %>
                <%= link_to content_tag(:span, "REGISTER"), register_challenge_path(@challenge), class: "btn" %>
                <%= link_to content_tag(:span, "ADD TO WATCH LIST"), challenge_path(@challenge), class: "btn" %>
                <br/><br/>
                We may also periodically contact registered members to provide them with challenge specific information, obtain feedback or additional input. You are not obligated to submit an entry however, we highly recommend that you register for this challenge if you have any interested in participating or would simply like to stay informed.

              <% elsif @current_member_participant.status.eql?('Watching') && !@challenge.closed_for_registration? %>
                <%= link_to content_tag(:span, "REGISTER"), register_challenge_path(@challenge), class: "btn" %>
                <br/><br/>
                Being registered for a challenge will allow you to receive all comments posted to the discussion board plus we may periodically contact registered members to provide them with challenge specific information, obtain feedback or additional input. You are not obligated to submit an entry.

              <% elsif @current_member_participant.registered? %>
                <%= link_to content_tag(:span, "SUBMIT YOUR ENTRY"), register_challenge_path(@challenge), class: "btn" %>
              <% end %>                    
            <% else %>
                <strong>Please login to submit for this challenge.</strong>
            <% end %>
            </div>
          <% end %>

        </p>
        <% end %>

        <h2>ADMINISTRATION</h2>
        <p>
          <a href="#">View submissions</a><br/>
          <a href="#">Edit this challenge</a>
        </p>

        <h2>TECHNOLOGIES</h2>
        <p>
          <%= platform_and_technology_tag_links(@challenge) %>
        </p>

        <h2>DEADLINE</h2>
        <p>
          <%= @challenge.end_date.strftime("%b %d, %Y at %H:%M %p") %>
          <br/>
          <% distance = Time.at(@challenge.end_date - Time.zone.now) %>
          <%= "Due in #{distance.day} days #{distance.hour} hours #{distance.min} minutes" %>
          <br/>
          <%= "Winner Announced on #{@challenge.winner_announced.to_s(:long)}" %>
          <br/>
        </p>

        <h2>PRIZES</h2>
        <p>
          Total: <strong><%= number_to_currency @challenge.total_prize_money, precision: 0 %></strong><br/>
          <% @challenge.challenge_prizes.each do |prize| %>
            <%= prize.place %> <strong><%= prize.prize %></strong>
            <br/>
          <% end %>
        </p>

        <h2>PARTICIPATION</h2>
        <p>
          <% if user_signed_in? %>
          Your status: <%= @current_member_participant.status %><br/>
          <% end %>
          <%= link_to pluralize(@challenge.participating_members, 'member'), "#" %> participating<br/>
          <%= link_to pluralize(@challenge.registered_members, 'member'), "#" %> registered
        </p>

        <% if @challenge.assets.present? %>
        <h2>ASSETS</h2>
        <ul class="unstyled">
          <% @challenge.assets.each do |asset| %>
            <li><%= link_to asset, "#" %></li>
          <% end %>
        </ul>
        <% end %>

        <h2>SHARE</h2>
        <ul class="unstyled inline">
            <li><a href="#" class="circle-orange"><span class="github">github</span></a></li>
            <li><a href="#" class="circle-orange"><span class="facebook">facebook</span></a></li>
            <li><a href="#" class="circle-orange"><span class="twitter">twitter</span></a></li>
            <li><a href="#" class="circle-orange"><span class="linkedin">linkedin</span></a></li>
        </ul>

        <h2>Chrome Extension</h2>
        <p>Install the <a href="#">CloudSpokes Chrome Extension</a> for quick and easy access to the latest
          CloudSpokes challenges, your payments, leaderboards and more!</p>
      </div>
    </div>
  </div> <!-- /.container -->
</div>

